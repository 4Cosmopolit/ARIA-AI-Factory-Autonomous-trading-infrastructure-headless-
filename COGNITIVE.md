# COGNITIVE.md — Когнитивная архитектура ARIA

> **Версия:** 14.05.2026  
> **Основа:** MemPalace, A‑MEM, ReasoningBank, Temporal Knowledge Graph, Palace Recall.

## 1. Иерархия памяти

| Уровень | Название | Содержание | TTL | Загрузка |
|---------|----------|------------|-----|----------|
| L0 | Identity | `AGENTS.md`, `RULES.md` | ∞ | всегда |
| L1 | Facts | `COGNITIVE.md`, `DECISIONS.md` | ∞ | всегда |
| L2 | Room | контекст конкретной задачи | сессия | по требованию |
| L3 | Deep | `memory-mcp` retrieval | N/A | поиск |
| HOT | Working | последние 10 событий | 24ч | всегда (лимит 10) |
| WARM | Decisions | архитектурные решения | 14д | по запросу |
| EPISODIC | Episodes | (причина→действие→исход) | 90д | поиск |
| COLD | Archive | старые данные | ∞ | архив |

## 2. Структурированная память (MemPalace)

- **Wings** – проекты / рынки (Bybit, Hyperliquid, arbitrage)
- **Halls** – типы памяти (facts, events, discoveries, preferences, advice)
- **Rooms** – темы (rate_limits, stop_loss, order_validation)
- **Closets** – суммаризация
- **Drawers** – оригинальные файлы

Поиск с фильтрацией Wing+Room даёт recall до 94.8% (против 60.9% без структуры).

## 3. Временной граф знаний (Temporal Knowledge Graph)

Каждый факт имеет поля:
- `subject`, `predicate`, `object`
- `valid_from`, `valid_to` (если `valid_to < now` – факт устарел)
- `confidence` (вес)

Запросы к графу автоматически исключают устаревшие факты.

## 4. Эволюция памяти (A‑MEM / Mem0)

- **Добавление:** LLM извлекает сущности и связи, сохраняет в векторную БД и граф.
- **Связывание:** автоматическое построение связей с историческими заметками.
- **Ревизия:** фоновый процесс пересматривает старые записи, обновляет описания и связи.
- **Забывание:** TTL‑кластеризация, Nudge Engine, верифицируемое удаление.

## 5. Palace Recall (метод loci)

При сохранении инцидента агент указывает `wing`, `hall`, `room`. При поиске – возможность фильтрации по иерархии.

## 6. Memory Warm‑Up

При старте сессии загружаются только L0 + L1 (≈170 токенов). Остальное – по требованию.

## 7. Инструменты работы с памятью (MCP)

- `memory-mcp` – основное хранилище, SPO‑триплеты, векторы
- `mem0-mcp` – гибридная память (векторы + граф с LLM‑экстракцией)
- `mem-palace-mcp` – 19 инструментов поиска, навигации, графов
- `a-mem-mcp` – эволюция памяти (ревизия, перелинковка)
- `notebooklm-mcp` – Zero‑token research, конвейер знаний

---

**Принцип:** ARIA не просто хранит – она организует, эволюционирует, забывает и учится на структуре.

git add COGNITIVE.md
git commit -m "ARIA-core: добавлен COGNITIVE.md — когнитивная архитектура, иерархия памяти"
git push origin main
