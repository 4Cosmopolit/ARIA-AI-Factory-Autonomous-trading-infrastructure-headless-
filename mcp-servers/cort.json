{
  "mcpServers": {
    "CoRT-chain-of-recursive-thinking": {
      "command": "pipx",
      "args": ["run", "cort-mcp", "--log=off"],
      "env": {
        "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}"
      }
    }
  }
}
